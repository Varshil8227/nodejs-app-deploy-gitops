apiVersion: v1
kind: Service
metadata:
  name: eventora-backend-svc
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: {{ .Values.backend.containerPort }}
      protocol: TCP
  selector:
    app: eventora-backend